;; package --- Summary
;;
;;; Commentary:
;;
;; This file contains defaults for variables as well as global keystrokes

;; (add-to-list 'load-path "~/Dropbox/.emacs.d/libraries/minimal")
;; (require 'minimal)
;; (minimal-mode t)
;; (nconc default-frame-alist '((cursor-type . bar)))
;; ;; Should the mode line be turned into a thin separator line?
;; (setq-default minimal-zap-mode-line nil)
;; ;; Should the scroll bars be removed?
;; (setq-default minimal-zap-scroll-bar t)
;; ;; Should the menu bar be removed?
;; (setq-default minimal-zap-menu-bar t)
;; ;; Should the tool bar be removed?
;; (setq-default minimal-zap-tool-bar t)
;; ;; Background colour for active mode line face when minimal minor mode is active
;; (setq-default minimal-mode-line-background "red")
;; ;; Background colour for inactive mode line face when minimal  minor mode is active
;; (setq-default minimal-mode-line-inactive-background "grey")
;; ;; Height of mode line when minimal minor mode is active
;; (setq-default minimal-mode-line-height 0.1)

(provide 'csb-minimal)

;;; csb-minimal.el ends here
